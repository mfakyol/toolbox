node_modules/
dist/
server/uploads/*
!server/uploads/.gitkeep
*.log
.DS_Store

# Secrets — never commit the real env file
.env

# Local-only dev bootstrap (in-memory Mongo) — not for the repo
server/dev-mongo.mjs
